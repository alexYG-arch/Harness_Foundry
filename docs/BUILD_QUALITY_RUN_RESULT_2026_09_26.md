# 搭建质量双案例运行摘要 · 2026-09-26

本页是维护者核对本机原始记录后整理的公开摘要，不是原始会话包、自签批准或所有项目的质量保证。
对应[搭建文档 v0.1](BUILD_QUALITY_RUN_TEST_BUILD_DOCUMENT_v0_1.md)和用户随后明确批准的“双案例搭建质量运行范围 v0.1”。
原文的草案状态保留为历史，实际确认和运行决定另记控制记录，不追改成事先已批准。

## 固定来源与环境

- Foundry 来源：`b48e3fcb513beca515f944ca477bd6a8eb846aaa`，35 文件通用包；两例全程使用同一包，运行后内容不变。
- 环境：macOS、Python 3.13.3、Codex CLI 0.155.0-alpha.16.4、请求模型 `gpt-6-astra`；有效服务内部版本未知。
- 输入：仓库公开合成 Task CLI PRD、CSV 项目说明和 starter；不是私人项目或完整商业 PRD。
- 流程：真实确认文档后准备和展示具体范围；取得后续真实批准后执行；没有逐任务批准、手工冻结令牌或额外 Hash 链。
- 没有改变正在测试的包、合同、独立检查器或预算；没有安装全局 Skill、访问真实业务数据或创建第三个工程。

## 案例结果

| 案例 | 实际模型任务 | LOCAL 实施 | 约定范围与结果 |
|---|---:|---:|---|
| Task CLI | 3 | 3 | A1–A6：Harness 建设、首次业务开发、故障副本修复及独立检查通过 |
| CSV 增量 | 2 | 1 | B1–B5：Harness 建设、实际新会话增量及独立检查通过 |

Task 使用 6/12 次、CSV 使用 3/6 次 implementation attempts，无预算重置或增加尝试。
两例均达到 `PLAN_CHECKS_ACCEPTED`；整体结论另外依据公开需求、原生会话和独立结果核对，
不改写通用 CLI 的 `harness_e2e_verified=false`。

Task 初次及修复后每轮均通过 25 项基础 CLI 观察和额外新实例；CSV 通过 13 项基础观察与
新输入/行置换关系检查。两例均实际运行内部副本与外部 workdir 的检查入口，分别每种布局
5、6 项项目测试通过，源码字节保持。CSV 原 summary/API、原测试及 USER_NOTES 均保留。
Task 原子存储实现和相关运行检查已核对，不外推断电或并发事务保证。

## 关键机制：实际触发了什么

| 场景 | 本次观察 | 不应外推的结论 |
|---|---|---|
| B01 方案推导 | 完整来源、需求映射、职责分工及真实计划经宿主核对 | 不由另一模型一句 PASS 证明语义完整 |
| B02 搭建和使用 | 两例都由真实新会话读取 Harness 规则并运行检查入口 | 不仅是文件生成或使用记录自报 |
| B03 接口组合 | 同一检查入口、绑定解释器、内外 workdir 实际可用 | 不代表全部平台或任意工具链 |
| B04 新实例与关系 | Task 新数据、CSV 行重排及诊断重算通过 | 输入公开可读，不是秘密盲测或统计泛化实验 |
| B05 实际修复 | 故障副本先被独立拒绝，另一模型会话修改 repaired 后完整复验通过 | 故障为事先公开注入；控制器 REJECTED 自动返修分支未自然触发 |
| B06 中断恢复 | 完整原生结果耐久保存后宿主 exit 86；新宿主恢复原 attempt，模型实施未重放 | 不承诺任意未知副作用自动恢复 |
| B07 完成停止 | 两例完成后再次 advance；attempt、事件和声明产物字节不变 | 不自行追加优化或第三案例 |
| B08 必须暂停 | 复用合同缺口、检查基础设施故障、到期/撤销的离线控制器/本地回归 | 不是本轮 5 次付费模型任务实际触发全部错误 |
| B09 同版复用 | Task 初始开发与缺陷修复在不同会话使用同一字节版本 Harness | CSV 没有第二开发任务；不同数据调用不算跨任务复用 |

初次独立业务检查两例均通过；这不表示模型内部没有失败命令或调试。
Task 原始正常实现、公开故障输入、修复结果和首次/最终观察均分别保留；观察器未撤销故障制造成功。
两次 Task 使用的固定三文件 Harness 从首次使用前到最终检查后完全相同。

## 成本与计时口径

5 次模型任务的客户端报告合计 input_tokens 2,036,547（其中 cached_input_tokens 1,727,232）、
output_tokens 37,343（其中 reasoning_output_tokens 1,776）。缓存/推理字段不再额外相加，费用未知，不填零。
这是单次范围记录，没有旧包/新包或多模型同条件对照，不主张性能收益。

两条业务实施命令的墙钟跨度约 1729/1723 秒；本机电源记录确认期间有合盖等系统休眠。
配置保持 900 秒的包内单调时钟超时，原生结果均为 exit 0、timed_out=false、无采集错误；
没有保存精确单调时钟起止值，不能声称墙钟小于 900 秒或精确模型计算时间。
当前计时机制不是包括系统睡眠的墙钟 SLA。全部最终控制事件在批准的 14:00 硬到期之前。

## 证据保留与结论

本机运行记录包括 `RUN_RESULT_v0.1.md`、`RUN_EVIDENCE_v0.1.json`、
`TIMING_REVIEW_v0.1.md`、逐案例观察、完成重入结果、原生会话附件及控制库。
它们保存在当次隔离运行根，不上传本机绝对路径、原始会话、权限记录、数据库或完整运行目录。
因此本页是公开摘要，不声称读者仅凭此页可独立复核全部原生记录。
公开的输入、Run Test 方法及适配器可用于另行准备一次有真实批准的复验。

结论：本次已审双案例及约定 B01–B09 覆盖义务通过，范围内无已知未解决的业务或搭建缺陷。
未自然触发的运行分支仅保持其原有源码回归结论；不自动升级为本轮实测证明。
这不修改历史发行验收、不代表新版本已发布，也不证明所有未来项目无缺陷。
