# Foundry 源码更新说明 — 2026-09-20

本次同步此前尚未推送的启动链、原生执行与独立验证提交，以及本轮通用建设和运行可靠性
改动。产品版本保持 `0.2.0`，协议目标保持 `2.9`；没有升级版本、创建发行 Tag 或发布安装包。
这是工程更新，不是目标 Harness、完整 PRD 或通用发行包的验收结论。

## 1. 通用建设入口

- 新增领域无关的 Requirement/Plan 编译、完整本地来源读取和来源定位校验。
  不注入视频、指定 Skill 或固定业务阶段；读取完整文件不等于自动完成语义审查。
- 所有 Harness 搭建需求保留澄清问答，先输出版本化搭建文档，用户 Review 并明确确认后
  才能进入 Foundry。共享门禁覆盖通用与旧兼容入口，文档变更使旧确认失效；旧委托、
  后台 PASS 和代理自批不能替代人工 Review。真实消息和语义范围由可信聊天宿主核对。
- 文档确认与运行批准分开。公开 CLI 支持提案、来源捕获、范围准备、批准、撤销、推进、
  未知结果处置和只读状态恢复。范围准备不运行目标；一次真实范围批准覆盖预算内的
  实施、独立检查、修复和后继推进，无逐 Workpack 人工门。
- 新控制流复用 SQLite，采用 `REVISION_V1`、事务和幂等请求，不新增事件/消息 Hash 链；
  仅对指定验证器和产物保留必要字节身份检查。历史数据库不自动迁移或继承批准。

入口及合同：[Build CLI](GENERIC_BUILD_CLI.md)、[Plan](GENERIC_BUILD_PLAN.md)、
[来源](GENERIC_SOURCE_INTAKE.md)、[Runtime](GENERIC_BUILD_RUNTIME.md)。

## 2. 执行、验证与恢复修复

- 原生 Codex/local 接收器持续排空 stdout/stderr；机器协议直接从原始字节增量解析，
  与有界人读摘要分离，避免长输出和跨块中文导致终态丢失。真实超限和落盘失败仍停止。
- 命令计划、观测和验收使用同一控制库；完整宿主附件可用于恢复未提交观测，部分 Case
  只续跑未派发的检查，不重复已完成实施。进程结束不等于业务验收，未知效果不盲目重放。
- 独立验证的基础设施失败与业务断言失败分流：检查器崩溃、依赖缺失或未分类失败停止，
  不驱使模型重写业务；正常业务失败才进入批准预算内的修复。控制器提交验收后才推进后继。
- 声明产物变更按依赖关系失效相关任务，保留无关分支、历史和预算；不以清空历史“恢复”。
- 新增 `executable://NAME` 参数引用，将同一 scope 中原始解释器路径传给二级进程，
  保留 venv/别名身份。未知引用在准备及重规划时拒绝，不猜 PATH、不隐式扩权。
- 补充原生离线预检要求：二级进程、实际依赖、场景写域及零写限制。单测或沙箱
  `true` 探测不能证明真实执行链准备完成；发现依赖缺口后仍需明确运行范围。

此前 17 个待同步提交还覆盖 Candidate 原生启动、耐久命令观测、编码协议、Lab 协议基础、
原生独立检查以及验收证据消费。这些属于兼容链路能力，不能替代通用 Harness 端到端验收。

## 3. 验证证据与复跑

2026-09-19 最后一次完整源码回归：**868 项，849 通过，19 项 opt-in 跳过，0 失败**，
耗时 764.169 秒。原生本地接收器组另行启用实测，20/20 通过；通用控制器 50/50 通过。
SQLite 连接清理仍有 `ResourceWarning`，不宣称零警告；跳过项不算实际验收。

同轮规范校验 PASS（101 文件），仓库 Skill 校验 PASS（5 个引用）。私人检查器自测和
离线 Case 预检仅作为本地诊断，不作为公开可复现测试或 M1 通过证明。

2026-09-20 推送前未改生产源码或测试；更新说明、忽略规则及能力清单后，复跑 91 项
定向测试全通过，官方核心校验 PASS（41 能力 / 26 selectors），规范与 Skill 复验通过。
完整 868 项结果沿用上述 09-19 的稳定源码回归，并非今天再次执行全套。

在安装项目声明的开发依赖 `.[test,security]` 的 Python 环境中运行：

```bash
PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py' -v
python3 tools/hffactory.py verify-spec --json
python3 tools/validate_skill.py
python3 tools/hffactory.py validate-core --json
```

原生测试需要显式配置可用的 Codex 接收器和依赖读取范围；未配置时按 opt-in 跳过，
不得把普通回归通过改称所有宿主、实际模型或干净安装均已验证。

测试入口限制：部分新增测试使用同级辅助模块导入。本次直接按 `tests.*` 模块名选择且
仅设 `PYTHONPATH=src` 时出现 4 个导入错误；补全为 `PYTHONPATH=src:tests` 后，以上
91 项全部通过。完整套件使用上面的标准 `discover -s tests` 入口；未修改测试来隐藏此限制。

## 4. 尚未闭合

- 真实 M1 已尝试，最新流程在 A 的独立检查基础设施失败处停止，A 未验收，B/C 未启动。
  源码修复没有自动重跑目标、替换已绑定检查器、重置预算或授予新增依赖；旧运行范围已到期。
- 旧专项兼容代码与包引用尚未完全退役，不能宣称当前仓库已是纯通用发行包。
- 任意环境故障自动恢复、主动取消、进程身份/启动空窗及已覆盖历史版本的重建仍有剩余义务。
- 同一干净包的代表性案例、安装后新 Codex 会话实际使用、第二案例和他人接入验收未关闭。
  `release_ready=false`；既有 41 项核心完成不等于上述新目标完成。

后续工作继续按[通用计划](FOUNDRY_GENERIC_CODEX_HARNESS_UPDATE_PLAN_v0_2.md)及
[可靠性工作包](FOUNDRY_RUNTIME_RELIABILITY_WORKPACK_EXECUTION_v0_1.md)推进，实际状态见
[Tracker](V2_9_IMPLEMENTATION_TRACKER.md)。本次 Git 推送不授予任何目标运行权限。

## 5. 公开范围

仅提交 Foundry 源码、回归测试、通用文档和仓库 Skill。`authoring_inputs/`、
`authoring_requests/`、`runs/`、`build/`、`dist/` 不进入本次提交；私人 PRD、搭建文档、
实际授权请求、控制数据库、命令原始附件和目标产物留在本地，不因更新说明而公开。
