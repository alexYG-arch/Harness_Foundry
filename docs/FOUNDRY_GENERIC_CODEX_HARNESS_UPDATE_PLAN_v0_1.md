# Foundry 通用 Codex Harness 修复与更新计划 v0.1

历史草案：当前计划已更新至 [v0.2](FOUNDRY_GENERIC_CODEX_HARNESS_UPDATE_PLAN_v0_2.md)，新增完整 PRD／项目说明驱动的 Coding Harness、最小 Hash 白名单与非作者 Codex 接入验收。下文保留为规划历史，不是当前实施顺序。

日期：2026-09-18。状态：`GOAL_CONFIRMED / PLAN_PROPOSED / IMPLEMENTATION_NOT_STARTED_FOR_THIS_PLAN`。

本文整合当日讨论，作为下一轮工程的计划入口。目标已由用户确认；具体实现方案、工作包和代表性项目是本计划的建议，不表示代码已实现、验收已通过或运行权限已授予。本文不创建 Program、Candidate、Execution Root，不修改既有锁、授权、状态库或历史产物，不执行 Workpack、安装或发布。文档版本 v0.1 不等于 Foundry 产品版本；产品版本号在发行兼容性核对后确定。

## 1. 目标与完成定义

完善并发布通用 Foundry：保留和验证现有生成、启动及核心能力，补齐授权范围内的 Workpack 自主执行、独立验证、推进和恢复；通过代表性 Harness 工程的真实端到端验收，证明发布包能够搭建符合明确需求、完整可用、验收范围内无已知未解决缺陷的 Harness，并向后续工程提供同样的验收与缺陷闭合机制。

Foundry 和目标 Harness 都以 Codex 为载体，由 Agent 主导任务理解、规划和实施。GPT-6 Astra 是本轮主要验证模型；本地 Python 等程序承担适合确定性执行的处理、校验和状态操作。不要求每个 Stage 调用模型，不要求每个 Harness 启动独立服务，也不为普通局部步骤强制创建子 Agent。

交付必须同时满足三个层次，不能互相替代：

1. Foundry 核心及生成、启动能力保持正确。
2. Foundry 可以在明确授权内真实构建、验证、修复并交付目标 Harness。
3. 目标 Harness 按正式使用方式加载后，能由 Codex Agent 完成约定任务，实际输出通过验收。

“完整可用”限定于公开支持的输入、宿主、依赖与验收范围；不承诺消除所有未知缺陷。范围内已知缺陷必须关闭，不能降级、隐藏或临时删掉验收要求以获得发布 PASS。

### 1.1 范围与非目标

- 用户补充确认：通用 Foundry 必须剔除已有专项，不限于视频。从生产路径、合同、指令和发布包中移除项目专属实现，不以“默认关闭”或改成可选插件作为剔除完成。视频、TTS、Motion、FFprobe、图片效果、公共 Skill 转视频均在剔除范围；本轮不继续 Skill Video Harness 工程。
- 保留上述工程暴露出的通用能力：需求追踪、任务依赖、Job 归属与读写边界、产物身份、真实执行观测、独立验收、失效传播与恢复。
- 历史 Candidate、Program、授权、来源快照和旧证据保持原样。通用化不意味着重新批准旧项目，也不要求继续支持已移出范围的视频运行时。
- 不建设多租户平台、跨组织认证网络、分布式调度集群或通用插件市场；不以全量攻防、重复 Hash 或更多仪式性文件作为完成信号。
- 不重写 Codex 已提供的工具循环、上下文管理和交互能力。新增组件必须对应现有闭环的具体缺口。

### 1.2 专项剔除清单与边界

| 剔除对象 | 处理方式 | 必须保留的通用能力 |
|---|---|---|
| 视频/TTS/Motion/Render/Demo 等专项产物、算子、阈值和阶段 | 从生产器、Validator、Schema、Prompt、默认配置与发行清单一并移除 | 可声明的产物、关系约束、实际运行观测与验收 |
| 公共 Skill URL、指定 GitHub 仓库、特定 Skill 名称和入口作为必选业务 | 删除固定注入及专用运行接口；不在本轮建设另一个 Skill 解释器产品 | 来源读取、来源归属、依赖与用户声明的输入合同 |
| 旧目标 ID、私有字段、历史 epoch 数值触发生产行为 | 移除专项分支，以显式通用能力/合同版本决定行为；历史状态不重写 | 版本识别、合法迁移、需求与运行隔离 |
| 固定视频 12 阶段，以及硬编码项目/Workpack/Job 名称 | 阶段与任务来自当前需求和生成结果，不由某个历史案例决定 | 依赖图、任务归属、失败返回和恢复 |
| 固定 Lab/Linkage/Main 三工程或专项认证作为所有项目的前置 | 取消强制项目形态及具体义务 ID 的绑定；按真实需求设置验证边界 | 实施与验收职责分离、控制器提交、真实产物关联 |
| 旧专项说明、测试材料和回执混入通用发布包 | 历史资料仅用于非运行档案；将有通用价值的失败形态改写为领域无关回归 | 缺陷来源与回归追踪；不丢失通用错误的检测能力 |

W1 应形成逐项删除/通用化/归档处置，不只搜索删除关键词。验收同时检查源代码依赖、生成结果与发行包：普通新需求不能触发专项入口；包中不能残留可被调用的专项实现。专项回归分为“转为通用反例”与“随专项退役”，说明依据，禁止借清理专项删除仍适用的核心/行为测试。

“通用”也不等于建立无限可扩展框架。主工程和留出工程的业务代码与测试留在各自目标工程，不反向写入 Foundry 核心；以后新项目通过需求、任务合同和本地实现接入。

## 2. 当前基线与诊断依据

本次核对的 HEAD 为 `f70c3de`，另有 9 个既存修改文件，涉及协议证据、独立校验、执行观测与测试；这些改动不因本计划而被撤销、提交或认定为完整验收实现。私有 `authoring_inputs/`、`authoring_requests/` 不进入发行物。

| 当前事实 | 已核对入口 | 对计划的影响 |
|---|---|---|
| 41 项核心能力的完成定义排除了真实构建和安装 | [现有 Tracker](V2_9_IMPLEMENTATION_TRACKER.md)、[README](../README.md) | 核心 PASS 保留；增加“构建能力”和“目标运行能力”验收，不能沿用旧完成标签作为总体完成 |
| 需求缺口检查覆盖字段、关系、声明的冲突与问题 | [service.py](../src/harness_foundry_factory/service.py)，`_requirement_gaps` | 增加目标与验收是否明确的语义审查；字段齐全不能证明需求明确 |
| 部分显式产物合同包含固定视频产物和 12 阶段序列 | [semantic_contracts.py](../src/harness_foundry_factory/semantic_contracts.py) | 从 Producer 到 Validator 一起解耦，不能只隐藏默认入口 |
| 旧提案把模型限制在冻结的决策槽内 | [R8 提案](HARNESS_FOUNDRY_V2_9_SEMANTIC_CONFORMANCE_UPGRADE_PLAN.md)，第 5–6 节 | 将稳定的验收/权限边界与可调整的实施计划分开 |
| 已有 `codex exec` 编码适配器；默认模型可为空，很多可选能力关闭 | [coding_process.py](../src/harness_foundry_factory/coding_process.py)、[编码协议](CODING_COMMAND_PROTOCOL.md) | 保留并验证适配器，明确模型与有效配置；不把 CLI 参数存在当成真实 Astra 运行证据 |
| 编码前置条件目前只支持首个启动后任务，运行入口按单一适配器模式选择 | [coding_runtime.py](../src/harness_foundry_factory/coding_runtime.py)、[cli.py](../src/harness_foundry_factory/cli.py) | 接通 Codex、本地处理、校验与验收之间的连续推进 |
| 完成审计仍返回未实现的验收/提交缺口，不发布后继能力 | [workpack_acceptance.py](../src/harness_foundry_factory/workpack_acceptance.py) | 实现独立判定与控制器验收提交，不能改为“没有报错就 PASS” |
| 已有持久化 attempt、观测和恢复；缺失观测时安全停住但未完整协调进程结果 | [control_kernel.py](../src/harness_foundry_factory/control_kernel.py) | 保留防重复机制，补齐受支持进程的结果核对与修复后新 attempt |
| 可移植包目前主要做版本和诊断启动冒烟，生成还声明 v2.8 规范依赖 | [portable.py](../src/harness_foundry_factory/portable.py) | 发布验收要从实际包完成真实建设；依赖显式随包提供或可复现取得，不能靠开发机的隐含目录 |

已读取本地既有完整测试日志：`Ran 717 tests ... OK (skipped=17)`。这是原工程测试日志的事实，不是本计划下重新执行的回归，更不是 717 项 Harness 验收。新计划的真实模型、安装、恢复和留出工程验收均为 `NOT_RUN`。

综合以上证据，本轮重点处理四类结构性问题：完成定义过窄、领域合同与通用核心耦合、推理实施被过细锁定、执行与验收链未闭合。它们是此次计划的工程诊断，不声称解释了所有历史问题。

## 3. 目标架构：稳定边界，可调整实施

| 责任层 | 应负责 | 不应负责 |
|---|---|---|
| Codex 宿主 | Agent 会话、工具执行载体、平台权限、宿主已有的上下文与交互能力 | 为 Foundry 自动认可产品验收 |
| Astra Agent | 需求理解、必要澄清、架构与代码设计、局部规划、实现、调试、范围内修复 | 扩大授权、改写用户目标、删减验收或自行宣布权威 PASS |
| Foundry 通用控制 | 工程身份、范围、依赖、允许的操作、执行观测、验收提交、预算与恢复 | 固定每一步推理或为每个工具动作另建一套编排 |
| 本地处理/验证组件 | 确定性解析、转换、计算、检查和真实结果观测 | 以模型生成的回执替代实际运行 |
| 目标 Harness | 业务规则、阶段合同、Agent 指令、所需本地模块及验收入口 | 依赖开发机私有路径或反向要求修改 Foundry 核心 |

### 3.1 宪章和合同保留到什么程度

使用精简且可执行的宪章：目标、非目标、输入输出、重要约束、权限、完成条件、失败与恢复原则。可机器检查的规则绑定真实执行/验收点；语义规则保留可观察的验收方式。不能把无法形式化的业务判断伪装成 Schema 或 Hash 检查，也不把每条普通建议升级为强制 Gate。

复用现有 Requirement、Architecture、Parent、Workpack、Job、Artifact 与事件存储模型，仅补齐闭环必要字段。实施前明确以下最小边界，不新建并行权威库：

- 用户需求和验收义务：决定必须完成什么。
- 建设授权：决定在哪里、用什么权限和预算执行哪些类别的操作。
- 可修订实施计划：决定当前如何完成；记录版本、依赖和变更理由。
- Stage/Workpack 合同：输入、产出、前置依赖、完成条件与失败返回。
- 验收记录：绑定真实执行、被验产物、判定和有效性；由控制器提交。

### 3.2 两种变更不能混为一谈

**可自主调整：** 内部模块、代码结构、算法、局部工具调用顺序、修复方法，以及不改变外部合同的任务拆分/合并/重排。Astra 提议后由控制器检查依赖和授权，接受的计划修订进入现有事件流。

**需要用户决定：** 目标或输出承诺变化、验收标准弱化、数据外发范围扩大、预算上限突破、覆盖真实用户数据等。模型不能将这些变更包装成“实现细节”。

业务承诺中的固定 Stage 仍是合同；它内部的实施步骤不是固定 Stage。已验收前置能力若因修改失效，相关后继等待复验；不能以动态计划为由绕过依赖。实现代码变化不自动触发全项目重新冻结或申请新 output root。

## 4. 需求澄清：不擅自补产品目标，也不追问实现细节

将需求信息区分为用户确认事实、来源可核对事实、显式假设、Agent 实施选择和未决问题。标记是信息来源说明，不要求为每个句子生成单独回执。

进入建设前，至少能回答：服务谁、解决什么、输入输出是什么、允许哪些工具/数据处理、怎样判定成功，以及何时必须停止。对影响结果的缺口，先查现有资料，再问最多三个最高价值问题；允许同时推进与缺口无关的已授权工作。

| 情况 | 处理 |
|---|---|
| “文档 Harness”但未说明检索、整理还是审批 | 给出具体差异并澄清，不能直接选业务目标 |
| 字段齐全但“质量好”等验收不可判定 | 提议可观察的例子/指标，待用户确认，不将空泛描述视为闭合 |
| 已明确本地文档检索、出处和禁止编造 | 索引方式、模块拆分等交给 Agent 自主决定 |
| 可逆且不影响范围的小细节 | 使用合理默认值；有交付影响的假设在简短 Readback 中说明 |
| 建设中发现新的关键冲突 | 停止依赖该决策的路径，保留已完成工作，不偷偷扩大范围 |

澄清结果写回现有 Requirement 状态；不能只留在聊天里。问题关闭必须有用户答案、来源证据或合法的实施决策依据，不能把 `OPEN` 改为 `RESOLVED` 就算闭合。回归应覆盖“资料可自行回答”“确实需要用户”“合理默认无需追问”“用户修正旧假设”四类行为。

## 5. Astra 与授权更新

### 5.1 指令与执行方式

审查 `AGENTS.md`、Foundry Skill、任务 Prompt 和生成给目标 Harness 的指令，删除重复、相互矛盾、要求逐步许可或把第一次实现当终点的条款。Skill 采用简短入口和按需加载；业务约束、工程操作建议、历史说明明确分开。

完成条件写为“实现、运行、观察结果、修复失败、验收并交付”，不是“输出实现方案”或“第一轮代码写完”。记录必要决策理由、假设、改动与证据，不要求记录模型内部思维链。

优先复用现有 Codex CLI 接口，先解决真实调用、观测与接续问题。只有它无法满足已确认的会话/事件需求时，再针对性评估 SDK 或 App Server；不同时建设多种宿主实现。明确绑定实际使用的 `gpt-6-astra`、CLI 版本与有效权限配置，不以 `model=null` 声称已验证 Astra，不静默回退到其他模型。

本轮不要求迁往裸 Responses API。异步工具、运行中指令更新等 API 功能不能推断为当前 Codex 接口已具备；仅在宿主确认支持且有实际需要时纳入。并行 Agent 不是默认必需；用户/工程授权允许且任务可独立时才启用，避免通过并行放大写域或成本。

### 5.2 连续建设授权，保留真实决策点

建议扩展现有 Parent 机制，让一次可读的建设授权覆盖明确范围内的生成、启动、模型服务调用、实施、本地验证、修复、接续及隔离安装验收。机器为具体 attempt 派生更窄的作用域；不要求用户手输 Hash、逐节点回复“继续”或重复批准同一风险。

这是一项待实现协议变化：现有预构建委托、离线 Parent 和编码 Parent 不会因本文而自动合并、扩权或获得新权限。继续保留单独的只读检查、仅 Authoring 和显式完整 Build 使用方式；正常启动 Foundry 不应自行创建目标工程。

授权至少说明目标和根目录、读写与网络/凭据范围、模型服务流量、可执行操作、时间/调用/修复预算、停止条件及有效期。模型服务联网与本地命令联网分别说明；“本地 Harness”不等于发送给 Codex 服务的任务内容完全不出机。代表性验收使用可公开的合成材料，不读取私有业务数据。

预算必须区分可强制执行的限制和仅可观测的统计。当前调用/时间限制不等于模型 token 或账单硬上限；宿主不能执行的预算保证不得写成已实现能力。需要硬上限时，先确认可用的宿主/服务机制再开始对应运行。

| 情况 | 授权内应自动处理 | 何时返回用户 |
|---|---|---|
| 范围内实现、测试、修复、复验、接续 | 连续推进 | 需要更改目标/验收或突破预算 |
| 临时错误或已知可恢复中断 | 有限重试、读取现有结果、恢复 | 副作用不能核实或原授权失效 |
| 隔离目录安装和验收 | 已包含于建设授权时连续执行 | 新增系统级安装、付费依赖或外发 |
| 目标实现文件变化 | 更新实际输入与受影响证据，重新验收 | 改变控制器/验收信任逻辑或授权范围 |
| 发布、推送、覆盖或删除真实资产 | 只准备可审阅结果 | 需要对应明确授权；不得沿用历史模糊批准 |

审批次数按实际风险变化解释，不机械要求每次工程恰好发生固定次数。超预算不能被“自动恢复”重置；授权撤销后阻止新派发，并准确报告已运行进程是否已终止、是否仍有副作用。

## 6. 实施工作包与依赖

下列工作包是计划单元，不是已生成或获准执行的目标 Workpack。除真实权限/需求变化外，实施授权生效后不在工作包之间设置额外人工关卡。

| 工作包 | 范围及主要落点 | 闭合条件 | 依赖 |
|---|---|---|---|
| W0 目标、基线与接口对齐 | 对齐 README、Tracker、产品 Manifest、活动提案、Skill；盘点 9 个既存改动；确定 §3 的最小接口和验收义务 | 新旧能力与缺口均有归属；已有结果不被改写；新目标未被标为完成 | 无 |
| W1 专项剔除与通用生成 | 按 §1.2 清理 Producer、Validator、Schema、Prompt、专项分支和发行清单；补语义澄清 | 逐项处置完成，源代码与包中无专项生产入口；两类非视频需求无需改核心即可生成；关键需求缺口会提问；旧通用生成/启动行为保留 | W0 |
| W2 Codex/本地统一执行 | `coding_process.py`、`coding_runtime.py`、`local_runtime.py`、CLI；统一任务/结果边界，实际 Astra 配置、输入与输出观测 | Codex 实际产出可用实现，本地步骤消费真实产物；异构步骤可衔接；未验收不发布能力 | W0 |
| W3 独立验收与提交 | `workpack_acceptance.py`、`workpack_evidence.py`、`project_verification.py`、现有控制内核 | 可声明并执行目标适用的验收；失败拒绝，成功由控制器提交；后继只消费有效验收 | W0 |
| W4 推进、修复和恢复 | 现有 DAG/控制内核/恢复组件；接通 W2、W3，支持有界修复和受影响失效 | 至少两个依赖 Workpack 连续完成；一次真实缺陷被拒绝并修复；中断恢复不盲目重放 | W2、W3 的最小闭环；W1 的通用任务合同 |
| W5 指令和授权简化 | Skill/AGENTS/Prompt、Parent/派生授权及停止解释；移除无必要的反复 Gate/检查 | 授权内无逐步确认；越界真实停止；授权撤销/过期有效；指令不压制已授权自主实施 | W0；与 W2/W3 共用接口 |
| W6 可移植交付 | `portable.py`、依赖/入口/安装说明；Foundry 包和目标 Harness 包 | 干净位置可从交付包运行建设与目标入口；无私有目录补丁、隐含 sibling、凭据或运行状态混入 | W0 后可做依赖清单；最终依赖 W1–W5 |
| W7 代表性验收与发布闭合 | 主工程、留出工程、缺陷账本、验收报告、发行说明 | §8–§9 全部满足；精确发行版本在承诺范围内无已知未解决缺陷 | 用例设计从 W0 开始；最终依赖 W1–W6 |

### 6.1 顺序和可并行范围

W0 先确定边界。随后 W1、W2、W3 可按文件归属并行，W5 的指令/授权设计及 W6 的依赖盘点、W7 的用例准备也可独立进行。修改共享 `control_kernel.py`、`cli.py`、合同定义和生成器的部分由一个集成负责人串行整合，不能让多个工作包各建状态机。没有并行 Agent 授权时，仍可按此依赖顺序单 Agent 完成。

**尽早纵向联通：** 在 W1/W2/W3/W5 的最小可用切片完成后，先做一个非视频工程的真实闭环：需求 → 生成 → Codex 实施 → 本地处理 → 独立验收 → 后继任务 → 失败修复 → 再验收。此时即可暴露宿主、授权和接口问题，不等待全部抽象层“完成”后才首次调用真实模型。该执行需要实际建设授权，计划本身不构成授权。

最小链路成功后，补完适用需求和 W4 恢复场景，再由 W6/W7 进行包级验收。W3 不得止于某一个 Lab 协议或三个固定义务，也不为通用化去实现一个覆盖所有行业的万能 Oracle；按项目合同绑定必要的可执行验证器。

### 6.2 W3/W4 的关键事务边界

1. 任务实施与目标写域不包含权威验收状态；Verifier 和预期结果不可被被验任务改写。
2. 现有控制器保留实际 attempt、过程观测及产物关联；Verifier 消费当前输入与结果，不接受任务自写 PASS。
3. 全部适用义务满足后，在现有 SQLite 事务中提交验收和产生的能力。验收不能成为依赖自身结果的任务命令。
4. 报告/回执文件从已提交状态投影；写文件失败可以重建，不重新执行任务。诊断临时副本不成为权威状态。
5. 修复使用新 attempt，保留失败原因与历史结果；仅失效受影响依赖。较新的失败、待完成或过期证据不能被旧 PASS 掩盖。
6. 区分“结果已观测未提交”“尚在运行”“已退出但观测缺失”“已提交但视图缺失”。先核实，后恢复；未知外部副作用不承诺 exactly-once，也不自动重放。

## 7. 回归、反漂移和比例适当的检查

### 7.1 覆盖表：每个目标要有真正的反例

| 目标 | 正向验收 | 必要反例/恢复 | 责任 |
|---|---|---|---|
| G1 保留核心/生成/启动 | 现有入口与普通通用输入仍正确 | 错误依赖/不支持的旧合同明确拒绝，不伪迁移 | W0/W1/W6 |
| G2 专项已剔除且无领域耦合 | §1.2 逐项处置；源代码和包中无专项生产入口；两个工程均不修改核心 | 去掉所有专项字段仍可建设；新业务不会重新硬编码进核心；不能靠空壳绕过行为义务 | W1/W6/W7 |
| G3 需求不漂移 | 问对关键问题并采用资料事实/合理默认 | 模糊目标、矛盾来源、未授权假设不能自动变成需求 | W1/W5 |
| G4 Codex 真实实施 | 模型事件和实际产物均可观察 | 模型自报完成但缺少功能时验收失败 | W2/W3 |
| G5 合同与产物有效 | Job、依赖、输入输出和被验对象一致 | 错 Job、缺产物、陈旧输入、部分完成不能通过 | W1/W3 |
| G6 独立判定 | 独立执行验证，控制器提交成功 | Schema 合格但行为错误、伪造回执、过期 PASS 都被拒绝 | W3 |
| G7 自主接续与修复 | 前置通过后推进，失败修复后重验 | 前置失败不推进；超预算不无限重试 | W4/W5 |
| G8 中断恢复 | 已完成工作复用，缺失视图重建 | 观测/提交前后中断不重复产生受保护效果 | W4 |
| G9 权限正确且不冗余 | 同一围栏内跨阶段连续工作 | 权限扩大、撤销、过期和未知副作用得到正确处置 | W5 |
| G10 包级交付 | 实际 Foundry 包建出可正式加载的 Harness | 缺少隐含开发机依赖时报告真实缺口，不能偷偷回原仓库取文件 | W6/W7 |

同时检查核心、生成器、独立 Validator 和真实运行路径，不能只改 Validator 或只修改 Fixture 让断言通过。模型辅助 Review 可以发现语义问题，但不能独自替代可观察的功能证据；同一 Chat 不声称外部独立认证。

### 7.2 Review/修复循环

每个发现保留最小记录：受影响需求、复现输入、实际/预期、根因所在层、修复、回归、影响的验收证据及当前状态。使用一个缺陷账本，不再复制多份相互竞争的状态。

循环为：复现 → 定位 Producer/控制/执行/验收根因 → 修复生产路径 → 加相关回归 → 复验受影响项 → 更新闭合状态。独立复核通过公共入口和真实输出检查，而不是照着实现复述 PASS 条件。

完成约定覆盖、必要回归及留出工程后，没有新改动、失败或具体疑点就不重复进行泛化“再找一轮问题”。新发现仍要记录并闭合；循环不以“永远找不到任何新问题”为终点，也不以审查轮数或测试总数作质量证明。

### 7.3 Hash、安全和测试成本

- Hash 用于必要的身份绑定、产物新鲜度、依赖复用、不可变交付和恢复判定，不用于证明内容正确；相同字节域不重复生成多套身份链。
- 改动仅重验真正受影响的依赖；不因普通报告或局部代码变化重建整个历史工程。
- 保留路径隔离、凭据保护、真实授权、失效证据、主要失败路径；不把个人本地工程升级为跨组织攻防平台。
- 默认以单一可信本地用户为威胁模型。历史明确要求的检查不在修复过程中静默取消；先确认适用范围和兼容性，再删除冗余。
- 验证是否值得增加，以“能发现哪个实际可达错误、发现后怎样改变处理”为依据。
- 当前仓库仍要求实现变更后运行完整 unittest；在明确更新工程指令前照常遵守。拟更新为开发中相关测试、集成里程碑完整回归、发行前完整回归与 E2E，避免每个微小编辑重复整套昂贵检查。

## 8. 真实端到端验收设计

### 8.1 建议的代表性工程

**主工程：本地文档检索与证据整理 Harness。** 使用合成 Markdown 文档和问题，Codex Agent 选择检索/整理动作，本地 Python 负责解析、索引或精确引用核对；交付带出处的结果。验收覆盖正常问题、无证据问题、来源更新、错误输入和恢复。至少两个存在真实依赖的 Workpack；不能用现成参考实现直接拷贝冒充 Foundry 建设。

**留出工程：CSV 质量诊断与修复建议 Harness。** 输入合成 CSV 与明确质量规则，Codex Agent 分析问题并组织建议，本地程序计算可重算的指标；不默认改写源数据。它验证不同数据结构和业务规则能复用同一 Foundry，而不是复制主工程模板。留出数据和预期检查与训练式调试用例分开；业务需求对建设者透明，隐藏的是评测答案而非需求。

以上是低依赖、非视频、可观察的建议，不构成已选定正式项目或运行授权。若换工程，仍须同时覆盖 Agent 主导和本地处理，不以纯脚本项目代替 Codex Harness 验收。

### 8.2 验收顺序

1. 从拟发布的 Foundry 候选包在隔离位置启动，按声明取得依赖，不读取原开发仓库补文件。
2. 通过公开入口输入明确需求，观察必要澄清和简短 Readback；授权实际根目录、模型服务及预算。
3. 由该包真实生成并构建目标 Harness，完成工作包依赖、独立验收和连续推进。
4. 引入一个有明确预期的目标功能缺陷，观察拒绝、范围内修复、复验及恢复推进；不能以 Mock 的“修复成功”代替。
5. 在受支持位置模拟中断/恢复，核对状态、结果与作用次数。尽量用低成本本地步骤测试中断边界，不反复消耗模型调用。
6. 从产生的目标交付包在另一个隔离位置按正式方式安装或加载，使用 Codex Agent 跑真实任务；检查最终结果而非只检查进程退出码。
7. 使用同一 Foundry 包完成留出工程。若必须改 Foundry，登记通用缺陷，修复后重建候选包并重验受影响项目；不得仍称旧包已通过。
8. 对最终发行物复核相关证据与字节身份，形成验收范围、环境、缺陷关闭和已知限制说明。

验收记录区分：本地单元/集成、测试进程、真实模型构建、目标实际运行、安装/加载后运行。某一层成功不替代下一层。必须执行的 Case 被跳过或工具不可用时记录 `NOT_RUN/BLOCKED`，不能合并成总 PASS。

## 9. 发布门、版本与停止条件

发行前全部满足：

1. 既有核心、通用生成和启动回归通过；产品 Manifest 和用户入口与实际能力一致。
2. G1–G10 均有适用证据，无缺失、陈旧或用静态检查冒充运行的项。
3. 实际 Astra/Codex 构建、连续 Workpack、独立验收、修复和恢复已观察到。
4. 主工程和留出工程从待发布 Foundry 包完成建设，并从各自交付包通过用户入口验收。
5. §1.2 的已有专项已从生产路径和发行包剔除，而非仅默认关闭；无核心改动才能接入普通新项目的隐藏要求，无开发机私有依赖。
6. 范围内已知缺陷全部关闭；范围外限制公开说明。若需要调整承诺范围，先由用户决定，不能自行削减后宣称原目标已达成。
7. 文档、安装方式、能力说明、产品版本和实际包一致；发布/推送按明确授权执行。

文档计划、代码提交、版本号提升、核心 PASS、Candidate 静态 PASS 和模型完成消息，都不能单独关闭本目标。不预先承诺 3.0；版本提升应由实际兼容性变化决定，不用版本号代替完成证据。

预算用尽、缺少真实环境/授权或无法消除的副作用不确定性是可解释的停止状态，不是“已完成”。可以继续推进不依赖该阻塞的工作，但不能以无限重试掩盖阻塞。

## 10. 迁移与回退

- v2.8 规范和来源快照继续只读；`V2_9_UPGRADE_MANIFEST.json` 作为历史导入边界，不重写以伪装当前实现状态。
- W0 检查并保留现有未提交改动；通用实现优先复用有效机制，不把整个协议证据切片机械复制成新通用框架。
- 旧 Authoring-only 路线、旧冻结和授权语义不自动迁移为 Build 权限；新路线必须有明确版本和能力检查。
- 历史媒体程序保留可审计状态；不要求新通用发布包继续执行它们，也不修改其 Candidate 来适配新版本。
- 新目标工程使用独立运行状态；对不兼容旧运行状态明确拒绝或提供受测试的显式迁移，禁止静默重解释。
- 每个合并切片可从版本管理恢复；测试使用独立临时根。发行不覆盖正在使用的版本，切换失败可回到上一已验证包；回退代码不撤销已发生副作用或复活已撤销授权。

## 11. 与旧计划的关系及实施前剩余选择

本计划取代下一轮规划中“继续视频专项建设”和“只完成启动链”的目标。旧 R8 的目标约束、来源追踪、确定性提交、风险授权与恢复原则继续复用；其固定 Semantic Slot、全量细粒度冻结、Authoring-only 完成边界需按本计划更新。历史计划和证据保留，不据此重解释旧授权。

W0 应建立一份目标/能力/测试/缺陷的简短映射并同步 README、Tracker、Manifest 与 Skill。以下选择不阻塞编写本计划，但在相应真实执行前必须落实：

- 首发支持的 Codex 版本、宿主和依赖范围，优先验证现有支持的本地环境，未测平台不声明支持。
- 代表性工程最终需求、隔离根目录、模型调用/数据范围与有限预算。
- 安装/加载的具体形式，以及最终发布目的地和版本号。

本轮只制定计划并添加导航说明，不改变当前 AGENTS/Skill 的执行权限、不运行目标项目、不提交或推送 Git。下一步首先是 W0 和最小接口对齐，再进入早期纵向闭环；不恢复旧视频 epoch 链。

## 12. 设计依据

2026-09-18 重新读取 OpenAI 官方 [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)：减少过细配方、按需加载指令，明确可持续完成的范围与终点。

同时核对 [GPT-6 Astra 模型指导](https://developers.openai.com/api/docs/guides/latest-model/gpt-6-astra.md#migration-quickstart)：澄清会改变结果的问题、审查指令冲突、按风险验证并减少不必要批准。其 API 特性不等同于本地 Codex 的现有支持。

本文的工作包拆分、架构边界、工程用例和发布门是结合本仓库现状作出的工程设计，不是官方对 Foundry 的认证，也不因模型更强而取消真实授权、结果验证和恢复边界。
